# Strings that still have no zh-Hant translation.
#
# Empty as of the v1.17 pass — every user-facing literal in the app is
# translated. The file stays because `test_localisation.py` reads it:
# a string that genuinely cannot be translated yet goes here with a
# reason, rather than being quietly skipped.
#
# The test also fails if a line here has since been translated, so the
# list cannot go stale.
